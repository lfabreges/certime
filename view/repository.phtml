<?php

$this->css = array(
    'css/repository.css'
);

$this->js = array(
    'js/repository.js'
);

?>
<div class="container-fluid">
    <div class="row-fluid">
        <div class="span4">
            <div class="well sidebar-nav">
                <ul id="snippets" class="nav nav-list">
                    <?php foreach ($this->themes as $name => $theme):
                        if ($theme->hasSnippets()): ?>
                            <li class="nav-header"><?php echo htmlspecialchars($name); ?></li>
                            <?php foreach ($theme->snippets as $name => $snippet): ?>
                                <li>
                                    <a href="#<?php echo urlencode($snippet->path); ?>">
                                        <?php echo htmlspecialchars($name); ?>
                                    </a>
                                </li>
                            <?php endforeach;
                        endif;
                    endforeach; ?>
                </ul>
            </div>
        </div>
        <div id="code" class="span8">
        </div>
    </div>
</div>
